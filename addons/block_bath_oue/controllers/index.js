angular.module('mm.addons.bath_oue')
/**
 * OUE controller.
 *
 * @module mm.addons.bath_oue
 * @ngdoc controller
 * @name OUEController
 */
.controller('OUEController',function($scope,$mmSite){
        var currentUserId = $mmSite.getUserId();

        function fetchOUE(currentUserId){

            //Display progress bar

            //Display complete now button

            //Department response

            //Better at bath logo

            //Prize draw promotion
        }
});